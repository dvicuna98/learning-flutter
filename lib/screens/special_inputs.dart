import 'package:flutter/material.dart';

class SpecialInputs extends StatelessWidget {
  const SpecialInputs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Special Input'),
      ),
      body: Container(),
    );
  }
}
